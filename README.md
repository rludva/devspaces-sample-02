# devspaces-sample-02
## Simple PHP example and devfile

Notes:
------
- TODO: Check simple git commit/git push inside DevSpaces and confirm that git variables like user.name and user.email a set properly..
- TODO: Apply the configmap in workspace-userdata-gitconfig-configmap.yaml and restart the workspace to have the gitconfig..
